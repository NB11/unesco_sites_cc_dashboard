# Track large Parquet file with Git LFS (not needed for GitHub Pages)
data/unesco_sites.parquet filter=lfs diff=lfs merge=lfs -text

# Track large CSV files with Git LFS (not needed for GitHub Pages)
data/climate_impact_data.csv filter=lfs diff=lfs merge=lfs -text
data/whc001.csv filter=lfs diff=lfs merge=lfs -text

# GeoJSON file stays in regular Git (needed for GitHub Pages, 8MB is acceptable)
# data/world-administrative-boundaries.geojson - regular Git

